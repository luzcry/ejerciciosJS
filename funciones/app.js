let Draw = function (numberOfTimes, whatToDraw) {
    for (let i = 1; i <= numberOfTimes; i++) {
        console.log(i + " " + whatToDraw);
    }
}
    Draw (10, "hey");
    Draw (6, "listen");
    Draw (4, "it's navy");

